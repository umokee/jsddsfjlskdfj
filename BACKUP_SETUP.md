# Backup System Setup Guide

Система автоматического бэкапа базы данных с интеграцией Google Drive.

## Возможности

- ✅ Автоматические бэкапы по расписанию
- ✅ Ручные бэкапы через веб-интерфейс
- ✅ Локальное хранение с автоматической ротацией
- ✅ Загрузка в Google Drive (опционально)
- ✅ Скачивание бэкапов через браузер
- ✅ История всех бэкапов с метаданными
- ✅ Индикатор статуса последнего бэкапа

## Быстрый старт (только локальные бэкапы)

1. **Перезапустите бэкенд** для применения миграций:
   ```bash
   sudo systemctl restart task-manager
   # или
   pkill -f "python.*backend/main.py" && python -m uvicorn backend.main:app --reload
   ```

2. **Настройте автобэкапы** в веб-интерфейсе:
   - Откройте `Settings` → вкладка `Backups`
   - Включите `Auto-Backup`
   - Настройте время и интервал
   - Сохраните

3. **Готово!** Бэкапы будут создаваться автоматически.

## Настройка Google Drive (опционально)

### Шаг 1: Установите зависимости

```bash
pip install google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client
```

### Шаг 2: Создайте Service Account

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google Drive API**:
   - APIs & Services → Library
   - Найдите "Google Drive API"
   - Нажмите "Enable"

4. Создайте Service Account:
   - APIs & Services → Credentials
   - Create Credentials → Service Account
   - Заполните имя (например, "task-manager-backups")
   - Нажмите "Create and Continue"
   - Пропустите роли (не обязательно)
   - Нажмите "Done"

5. Создайте ключ:
   - Нажмите на созданный Service Account
   - Keys → Add Key → Create New Key
   - Выберите JSON
   - Сохраните файл

### Шаг 3: Настройте сервер

1. **Скопируйте JSON файл** на сервер:
   ```bash
   sudo mkdir -p /etc/task-manager
   sudo mv ~/credentials.json /etc/task-manager/google-drive-credentials.json
   sudo chmod 600 /etc/task-manager/google-drive-credentials.json
   ```

2. **Установите переменную окружения**:

   Для systemd:
   ```bash
   sudo systemctl edit task-manager
   ```

   Добавьте:
   ```ini
   [Service]
   Environment="GOOGLE_DRIVE_CREDENTIALS=/etc/task-manager/google-drive-credentials.json"
   ```

   Сохраните и перезапустите:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl restart task-manager
   ```

   Для ручного запуска:
   ```bash
   export GOOGLE_DRIVE_CREDENTIALS=/etc/task-manager/google-drive-credentials.json
   python -m uvicorn backend.main:app --reload
   ```

### Шаг 4: Включите в настройках

1. Откройте веб-интерфейс
2. Settings → Backups
3. Включите "Upload backups to Google Drive"
4. Сохраните

### Шаг 5: Проверьте

1. Создайте тестовый бэкап:
   - Перейдите на страницу Backups
   - Нажмите "[+] BACKUP NOW"

2. Проверьте Google Drive:
   - Войдите в Google Drive с аккаунтом Service Account
   - ИЛИ: посмотрите логи сервера для подтверждения загрузки

## Настройки бэкапов

### В веб-интерфейсе (Settings → Backups):

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| Auto-Backup | Включить автоматические бэкапы | ✅ Включено |
| Backup Time | Время суток для бэкапа | 03:00 |
| Backup Interval | Интервал в днях (1 = каждый день) | 1 день |
| Keep Local Backups | Кол-во бэкапов для хранения | 10 |
| Google Drive | Загружать в облако | ❌ Выключено |

### Переменные окружения:

```bash
# Путь к credentials для Google Drive
GOOGLE_DRIVE_CREDENTIALS=/path/to/credentials.json

# Директория для бэкапов (опционально)
TASK_MANAGER_BACKUP_DIR=/var/lib/task-manager/backups

# Путь к БД (опционально)
TASK_MANAGER_DB_DIR=/var/lib/task-manager
```

## Использование

### Автоматические бэкапы

После настройки бэкапы создаются автоматически:
- Проверка каждую минуту
- Создание по расписанию
- Автоматическая загрузка в Google Drive (если включено)
- Автоматическая очистка старых бэкапов

### Ручные бэкапы

1. Откройте страницу **Backups** в навигации
2. Нажмите **[+] BACKUP NOW**
3. Бэкап создастся мгновенно

### Скачивание бэкапов

1. Откройте страницу **Backups**
2. Найдите нужный бэкап в списке
3. Нажмите **↓ DOWNLOAD**
4. Файл скачается в браузер

### Восстановление из бэкапа

```bash
# 1. Остановите приложение
sudo systemctl stop task-manager

# 2. Сделайте резервную копию текущей БД (на всякий случай)
sudo cp /var/lib/task-manager/tasks.db /var/lib/task-manager/tasks.db.old

# 3. Замените БД файлом бэкапа
sudo cp /path/to/backup_*.db /var/lib/task-manager/tasks.db

# 4. Запустите приложение
sudo systemctl start task-manager
```

## Индикаторы статуса

На странице Backups отображается индикатор:

- 🟢 **Зеленый**: Последний бэкап меньше 24 часов назад
- 🟡 **Желтый**: Последний бэкап от 1 до 7 дней назад
- 🔴 **Красный**: Последний бэкап больше 7 дней назад или отсутствует

## Структура бэкапов

### Локальное хранение

```
/var/lib/task-manager/backups/
├── backup_auto_2024-01-15_03-00-00.db
├── backup_auto_2024-01-16_03-00-00.db
├── backup_manual_2024-01-16_14-30-15.db
└── ...
```

### Google Drive

Создается папка "TaskManager Backups" с такой же структурой.

## Troubleshooting

### Бэкапы не создаются автоматически

1. Проверьте настройки:
   ```bash
   # В веб-интерфейсе Settings → Backups
   # Убедитесь что Auto-Backup включен
   ```

2. Проверьте логи:
   ```bash
   tail -f /var/log/task-manager/app.log | grep backup
   ```

3. Проверьте права доступа:
   ```bash
   ls -la /var/lib/task-manager/backups/
   # Должна быть возможность записи
   ```

### Google Drive upload fails

1. Проверьте credentials:
   ```bash
   echo $GOOGLE_DRIVE_CREDENTIALS
   # Должен показать путь к файлу

   cat $GOOGLE_DRIVE_CREDENTIALS
   # Должен показать JSON
   ```

2. Проверьте логи на наличие ошибок:
   ```bash
   tail -f /var/log/task-manager/app.log | grep -i "google\|drive"
   ```

3. Проверьте что Google Drive API включен в консоли

### Бэкапы не скачиваются

1. Проверьте что файл существует:
   ```bash
   ls -la /var/lib/task-manager/backups/
   ```

2. Проверьте права доступа:
   ```bash
   # Файлы должны быть читаемы пользователем, под которым запущен бэкенд
   chmod 644 /var/lib/task-manager/backups/*.db
   ```

## Безопасность

### Рекомендации:

1. **Защитите credentials**:
   ```bash
   chmod 600 /etc/task-manager/google-drive-credentials.json
   chown task-manager:task-manager /etc/task-manager/google-drive-credentials.json
   ```

2. **Ограничьте доступ к бэкапам**:
   ```bash
   chmod 700 /var/lib/task-manager/backups
   ```

3. **Используйте HTTPS** для веб-интерфейса

4. **Настройте Google Drive permissions**:
   - Service Account имеет доступ только к своей папке
   - Не делитесь credentials файлом

## Мониторинг

### Проверка статуса бэкапов

```bash
# Последние бэкапы
ls -lt /var/lib/task-manager/backups/ | head -5

# Размер бэкапов
du -h /var/lib/task-manager/backups/

# Логи бэкапов
grep -i backup /var/log/task-manager/app.log | tail -20
```

### Алерты

Настройте мониторинг на основе индикатора в веб-интерфейсе или логов.

## FAQ

**Q: Сколько места занимают бэкапы?**
A: Размер SQLite базы обычно 1-10 MB. 10 бэкапов ≈ 10-100 MB.

**Q: Можно ли использовать другие облачные хранилища?**
A: Да, можно расширить `backup_service.py` для поддержки S3, Dropbox, и т.д.

**Q: Бэкапы зашифрованы?**
A: Нет, файлы хранятся как есть. Для шифрования используйте шифрованную файловую систему.

**Q: Как часто делать бэкапы?**
A: Зависит от частоты изменений. Рекомендуется ежедневно (по умолчанию).

**Q: Google Drive безопасен?**
A: Service Account имеет ограниченные права и доступ только к своей папке.

## Дополнительная информация

- Автоматические миграции при обновлении
- Бэкапы не влияют на производительность
- Scheduler работает в фоновом режиме
- Поддержка множественных бэкапов одновременно
